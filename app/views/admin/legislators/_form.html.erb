<%= form_for([:admin, @legislator]) do |f| %>
  <% if @legislator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@legislator.errors.count, "error") %> prohibited this legislator from being saved:</h2>

      <ul>
      <% @legislator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :party_id %><br>
    <%= f.select :party_id, Party.all.collect { |p| [ p.name, p.id] }, include_blank: false, prompt: "請選政黨" %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :constituency %><br>
    <%= f.text_field :constituency %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

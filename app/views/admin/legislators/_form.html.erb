<%= form_for([:admin, @admin_legislator]) do |f| %>
  <% if @admin_legislator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_legislator.errors.count, "error") %> prohibited this admin_legislator from being saved:</h2>

      <ul>
      <% @admin_legislator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :party_id %><br>
    <%= f.number_field :party_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :constituency %><br>
    <%= f.text_field :constituency %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
